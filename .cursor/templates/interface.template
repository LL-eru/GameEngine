# Interface Template

Use for stable module boundaries.

`cpp
#pragma once

class I{{Name}} {
public:
    virtual ~I{{Name}}() = default;

    virtual bool Initialize() = 0;
    virtual void Shutdown() = 0;
};
`

Document ownership, threading, and whether calls may fail.

